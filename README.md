# Require this step before git add . --> git init
# Build: 
    1. danminimac$ npm run build --profile, or --debug
    2.  
# For .ts or .tsx files: npm install --save-dev typescript @types/react @types/node (tsconfig.json created automatically)